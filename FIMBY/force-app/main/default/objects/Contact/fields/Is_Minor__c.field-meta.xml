<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Is_Minor__c</fullName>
    <description>True when Birthdate is known and Age_Years__c is under 19. ADVISORY ONLY, for badge and UI gating. A blank Birthdate yields false, so this must never be used as the sole guard on a capability check; server-side guards test Is_Parent_Proxied__c and compute age in Apex.</description>
    <externalId>false</externalId>
    <formula>AND(
  NOT(ISBLANK(Birthdate)),
  Age_Years__c &lt; 19
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Is Minor</label>
    <trackTrending>false</trackTrending>
    <type>Checkbox</type>
</CustomField>
