<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Requester_Contact__c</fullName>
    <deleteConstraint>Restrict</deleteConstraint>
    <description>The person asking to be vouched (vouchee). Lookup (not MD) because Salesforce forbids two MD relationships from the same child object to the same parent.</description>
    <label>Requester Contact</label>
    <referenceTo>Contact</referenceTo>
    <relationshipLabel>Vouches as Requester</relationshipLabel>
    <relationshipName>Vouches_As_Requester</relationshipName>
    <required>true</required>
    <trackTrending>false</trackTrending>
    <type>Lookup</type>
</CustomField>
