<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Push_Events__c</fullName>
    <defaultValue>true</defaultValue>
    <description>Whether the user wants push notifications for event-related activity (event chat replies, event reminders, RSVP updates). Layered on top of the Activity push gate. Settings UI surfaces this as the Events row in the per-category notification matrix.</description>
    <externalId>false</externalId>
    <label>Push Events</label>
    <trackFeedHistory>false</trackFeedHistory>
    <type>Checkbox</type>
</CustomField>
